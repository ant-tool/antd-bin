#!/usr/bin/env node

var program = require('commander');
require('gnode');

program
  .option('-p, --port <port>', 'port')
  .option('--hot', 'enable HOT MODULE REPLACEMENT')
  .parse(process.argv);

require('../lib/server')({
  port: program.port,
  hot: program.hot
});
